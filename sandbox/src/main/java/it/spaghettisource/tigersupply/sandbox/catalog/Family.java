package it.spaghettisource.tigersupply.sandbox.catalog;

/**
 * The family an entity belongs to, which tells the test scene into which group a case's primary
 * entity is inserted.
 */
public enum Family {
	PROJECTILE,
	ENEMY,
	EFFECT
}
